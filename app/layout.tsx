import type { Metadata } from "next";
import { Analytics } from "@vercel/analytics/next";
import { ThemeProvider } from "@/components/shared/ThemeProvider";
import { TooltipProvider } from "@/components/ui/tooltip";
import { Toaster } from "@/components/ui/sonner";
import "./globals.css";

export const metadata: Metadata = {
  metadataBase: new URL("https://personalfinancedecisionengine.com"),
  title: {
    default: "Personal Finance Decision Engine — Your Next Best Financial Move",
    template: "%s | Personal Finance Decision Engine",
  },
  description:
    "Stop guessing with your finances. Get personalized recommendations to save smarter, invest better, and build wealth faster.",
  openGraph: {
    title: "Personal Finance Decision Engine",
    description: "Your money. Your goals. Your next best decision.",
    url: "https://personalfinancedecisionengine.com",
    siteName: "Personal Finance Decision Engine",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Personal Finance Decision Engine",
    description: "Your money. Your goals. Your next best decision.",
  },
  // Favicon and Open Graph image are generated by app/icon.tsx and
  // app/opengraph-image.tsx (Next.js metadata file conventions) - no need
  // to reference static files here.
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      suppressHydrationWarning
      className="h-full antialiased"
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem={false}
          disableTransitionOnChange
        >
          <TooltipProvider delay={200}>
            {children}
            <Toaster richColors position="top-right" />
          </TooltipProvider>
          <Analytics />
        </ThemeProvider>
      </body>
    </html>
  );
}
